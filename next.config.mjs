/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'res.cloudinary.com', 
      'i.ibb.co',           
      'img.garagepotti.xyz'
    ],
  },
};

export default nextConfig;
